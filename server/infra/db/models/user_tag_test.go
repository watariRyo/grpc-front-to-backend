// Code generated by SQLBoiler 4.14.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserTags(t *testing.T) {
	t.Parallel()

	query := UserTags()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserTagsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserTagsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserTags().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserTagsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserTagSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserTagsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserTagExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserTag exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserTagExists to return true, but got false.")
	}
}

func testUserTagsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userTagFound, err := FindUserTag(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userTagFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserTagsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserTags().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserTagsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserTags().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserTagsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userTagOne := &UserTag{}
	userTagTwo := &UserTag{}
	if err = randomize.Struct(seed, userTagOne, userTagDBTypes, false, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}
	if err = randomize.Struct(seed, userTagTwo, userTagDBTypes, false, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userTagOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userTagTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserTags().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserTagsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userTagOne := &UserTag{}
	userTagTwo := &UserTag{}
	if err = randomize.Struct(seed, userTagOne, userTagDBTypes, false, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}
	if err = randomize.Struct(seed, userTagTwo, userTagDBTypes, false, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userTagOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userTagTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func userTagBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func userTagAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserTag) error {
	*o = UserTag{}
	return nil
}

func testUserTagsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &UserTag{}
	o := &UserTag{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, userTagDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserTag object: %s", err)
	}

	AddUserTagHook(boil.BeforeInsertHook, userTagBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	userTagBeforeInsertHooks = []UserTagHook{}

	AddUserTagHook(boil.AfterInsertHook, userTagAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	userTagAfterInsertHooks = []UserTagHook{}

	AddUserTagHook(boil.AfterSelectHook, userTagAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	userTagAfterSelectHooks = []UserTagHook{}

	AddUserTagHook(boil.BeforeUpdateHook, userTagBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	userTagBeforeUpdateHooks = []UserTagHook{}

	AddUserTagHook(boil.AfterUpdateHook, userTagAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	userTagAfterUpdateHooks = []UserTagHook{}

	AddUserTagHook(boil.BeforeDeleteHook, userTagBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	userTagBeforeDeleteHooks = []UserTagHook{}

	AddUserTagHook(boil.AfterDeleteHook, userTagAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	userTagAfterDeleteHooks = []UserTagHook{}

	AddUserTagHook(boil.BeforeUpsertHook, userTagBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	userTagBeforeUpsertHooks = []UserTagHook{}

	AddUserTagHook(boil.AfterUpsertHook, userTagAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	userTagAfterUpsertHooks = []UserTagHook{}
}

func testUserTagsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserTagsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(userTagColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserTagToManyIncomeAndExpenditures(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b, c IncomeAndExpenditure

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, incomeAndExpenditureDBTypes, false, incomeAndExpenditureColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, incomeAndExpenditureDBTypes, false, incomeAndExpenditureColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.UserTagID, a.ID)
	queries.Assign(&c.UserTagID, a.ID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.IncomeAndExpenditures().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.UserTagID, b.UserTagID) {
			bFound = true
		}
		if queries.Equal(v.UserTagID, c.UserTagID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := UserTagSlice{&a}
	if err = a.L.LoadIncomeAndExpenditures(ctx, tx, false, (*[]*UserTag)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.IncomeAndExpenditures); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.IncomeAndExpenditures = nil
	if err = a.L.LoadIncomeAndExpenditures(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.IncomeAndExpenditures); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testUserTagToManyAddOpIncomeAndExpenditures(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b, c, d, e IncomeAndExpenditure

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, false, strmangle.SetComplement(userTagPrimaryKeyColumns, userTagColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*IncomeAndExpenditure{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, incomeAndExpenditureDBTypes, false, strmangle.SetComplement(incomeAndExpenditurePrimaryKeyColumns, incomeAndExpenditureColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*IncomeAndExpenditure{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddIncomeAndExpenditures(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.UserTagID) {
			t.Error("foreign key was wrong value", a.ID, first.UserTagID)
		}
		if !queries.Equal(a.ID, second.UserTagID) {
			t.Error("foreign key was wrong value", a.ID, second.UserTagID)
		}

		if first.R.UserTag != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.UserTag != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.IncomeAndExpenditures[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.IncomeAndExpenditures[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.IncomeAndExpenditures().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testUserTagToManySetOpIncomeAndExpenditures(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b, c, d, e IncomeAndExpenditure

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, false, strmangle.SetComplement(userTagPrimaryKeyColumns, userTagColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*IncomeAndExpenditure{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, incomeAndExpenditureDBTypes, false, strmangle.SetComplement(incomeAndExpenditurePrimaryKeyColumns, incomeAndExpenditureColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetIncomeAndExpenditures(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.IncomeAndExpenditures().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetIncomeAndExpenditures(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.IncomeAndExpenditures().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.UserTagID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.UserTagID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.UserTagID) {
		t.Error("foreign key was wrong value", a.ID, d.UserTagID)
	}
	if !queries.Equal(a.ID, e.UserTagID) {
		t.Error("foreign key was wrong value", a.ID, e.UserTagID)
	}

	if b.R.UserTag != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.UserTag != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.UserTag != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.UserTag != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.IncomeAndExpenditures[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.IncomeAndExpenditures[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testUserTagToManyRemoveOpIncomeAndExpenditures(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b, c, d, e IncomeAndExpenditure

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, false, strmangle.SetComplement(userTagPrimaryKeyColumns, userTagColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*IncomeAndExpenditure{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, incomeAndExpenditureDBTypes, false, strmangle.SetComplement(incomeAndExpenditurePrimaryKeyColumns, incomeAndExpenditureColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddIncomeAndExpenditures(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.IncomeAndExpenditures().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveIncomeAndExpenditures(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.IncomeAndExpenditures().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.UserTagID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.UserTagID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.UserTag != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.UserTag != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.UserTag != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.UserTag != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.IncomeAndExpenditures) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.IncomeAndExpenditures[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.IncomeAndExpenditures[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testUserTagToOneTagGroupUsingGroup(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserTag
	var foreign TagGroup

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, tagGroupDBTypes, false, tagGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TagGroup struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.GroupID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Group().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	ranAfterSelectHook := false
	AddTagGroupHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *TagGroup) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := UserTagSlice{&local}
	if err = local.L.LoadGroup(ctx, tx, false, (*[]*UserTag)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Group == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Group = nil
	if err = local.L.LoadGroup(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Group == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testUserTagToOneUserUsingUser(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserTag
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userTagDBTypes, false, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.UserID = foreign.UserID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.User().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.UserID != foreign.UserID {
		t.Errorf("want: %v, got %v", foreign.UserID, check.UserID)
	}

	ranAfterSelectHook := false
	AddUserHook(boil.AfterSelectHook, func(ctx context.Context, e boil.ContextExecutor, o *User) error {
		ranAfterSelectHook = true
		return nil
	})

	slice := UserTagSlice{&local}
	if err = local.L.LoadUser(ctx, tx, false, (*[]*UserTag)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.User = nil
	if err = local.L.LoadUser(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.User == nil {
		t.Error("struct should have been eager loaded")
	}

	if !ranAfterSelectHook {
		t.Error("failed to run AfterSelect hook for relationship")
	}
}

func testUserTagToOneSetOpTagGroupUsingGroup(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b, c TagGroup

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, false, strmangle.SetComplement(userTagPrimaryKeyColumns, userTagColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, tagGroupDBTypes, false, strmangle.SetComplement(tagGroupPrimaryKeyColumns, tagGroupColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, tagGroupDBTypes, false, strmangle.SetComplement(tagGroupPrimaryKeyColumns, tagGroupColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*TagGroup{&b, &c} {
		err = a.SetGroup(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Group != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.GroupUserTags[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.GroupID, x.ID) {
			t.Error("foreign key was wrong value", a.GroupID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.GroupID))
		reflect.Indirect(reflect.ValueOf(&a.GroupID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.GroupID, x.ID) {
			t.Error("foreign key was wrong value", a.GroupID, x.ID)
		}
	}
}

func testUserTagToOneRemoveOpTagGroupUsingGroup(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b TagGroup

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, false, strmangle.SetComplement(userTagPrimaryKeyColumns, userTagColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, tagGroupDBTypes, false, strmangle.SetComplement(tagGroupPrimaryKeyColumns, tagGroupColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetGroup(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveGroup(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Group().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Group != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.GroupID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.GroupUserTags) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testUserTagToOneSetOpUserUsingUser(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserTag
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userTagDBTypes, false, strmangle.SetComplement(userTagPrimaryKeyColumns, userTagColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetUser(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.User != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.UserTags[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.UserID != x.UserID {
			t.Error("foreign key was wrong value", a.UserID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.UserID))
		reflect.Indirect(reflect.ValueOf(&a.UserID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.UserID != x.UserID {
			t.Error("foreign key was wrong value", a.UserID, x.UserID)
		}
	}
}

func testUserTagsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserTagsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserTagSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserTagsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserTags().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userTagDBTypes = map[string]string{`ID`: `bigint`, `UserID`: `varchar`, `TagName`: `varchar`, `GroupID`: `bigint`, `GrantLimit`: `varchar`, `CreatedAt`: `datetime`, `UpdatedAt`: `datetime`}
	_              = bytes.MinRead
)

func testUserTagsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userTagPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userTagAllColumns) == len(userTagPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserTagsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userTagAllColumns) == len(userTagPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserTag{}
	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userTagDBTypes, true, userTagPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userTagAllColumns, userTagPrimaryKeyColumns) {
		fields = userTagAllColumns
	} else {
		fields = strmangle.SetComplement(
			userTagAllColumns,
			userTagPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserTagSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserTagsUpsert(t *testing.T) {
	t.Parallel()

	if len(userTagAllColumns) == len(userTagPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLUserTagUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserTag{}
	if err = randomize.Struct(seed, &o, userTagDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserTag: %s", err)
	}

	count, err := UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userTagDBTypes, false, userTagPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserTag struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserTag: %s", err)
	}

	count, err = UserTags().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
