syntax = "proto3";

package server;

option go_package = "github.com/watariRyo/balance/server/proto";

message UserTagListRequest {
    string user_id = 1;
}

message UserTagListResponse {
    repeated UserTagResponse user_tag_list = 1;
}

message UserTagRequest {
    int64 id = 1;
    string user_id = 2;
    string tag_name = 3;
    int64 group_id = 4;
    string grant_limit = 5;
}

message UserTagResponse {
    int64 id = 1;
    string user_id = 2;
    string tag_name = 3;
    bool has_group = 4;
    int64 group_id = 5;
    string grant_limit = 6;
}

message UserTagID {
    int64 id = 1;
    string user_id = 2;
}


service UserTagService {
    rpc ListUserTag (UserTagListRequest) returns (UserTagListResponse);
    rpc GetUserTag (UserTagID) returns (UserTagResponse);
    rpc RegisterUserTag (UserTagRequest) returns (UserTagResponse);
    rpc UpdateUserTag (UserTagRequest) returns (UserTagResponse);
    rpc DeleteUserTag (UserTagID) returns (UserTagID);
}